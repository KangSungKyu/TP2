# 2026년 8월 2주차 (2026-W32) 주간 종합 보고서

## 주간 개요

- 기간: 2026-08-03 ~ 2026-08-09
- 핵심 목표: Stage 1 MVP 플레이 흐름, Production HUD, 데이터 정규화, Portal·Room 접근성 완성

## 핵심 성과

1. Stage 1 MVP
   - 결정론적 3×4/4×3 그래프와 10개 활성 Room, 분기·순환·BossGate 흐름을 구현했다.
   - Room Chunk loader를 uint FK 기반으로 연결하고 빈 Room, 반복 Chunk, Portal 누락을 수선했다.

2. 전투 안정성
   - Player/Monster 중첩 피격 지면 관통, 장시간 knockback, Groggy·Death 중 stale 공격을 제거했다.
   - Monster death fade와 pool reuse, SuperArmor knockback 차단 계약을 확립했다.

3. 리소스·데이터 규격
   - Unit texture PPU100, 발 pivot, prefab `[type]_[idx]` 규격을 적용했다.
   - TextData en/kr, Skill/Effect Text FK, CSV bool 0/1, 공용 SolidFill UI sprite 계약을 반영했다.
   - BMJUA TTF/SDF를 도입해 한글 UI 출력을 복구했다.

4. Production UI
   - Player/Boss/Monster HUD 생명주기 bind/unbind와 HP/Posture/MP 표시를 구현했다.
   - Production Minimap과 12개 Room 상태 View, M 키 토글을 추가했다.
   - SortingLayer와 SortingGroup을 Unit/Effect/Tilemap/WorldUI 역할 기준으로 정규화했다.

5. 메트로배니아형 Portal 동선
   - 11개 Room, 44개 socket에 공용 Portal을 배치했다.
   - 고지대 solid landing, one-way 접근 발판, 안전 EntryMarker를 구성했다.
   - stale owner/generation 전환과 target 7 경고를 제거했다.

## 최종 검증

| 게이트 | 결과 |
|---|---:|
| 전용 계약 | 4/4 PASS |
| EditMode 제품 게이트 | 112/112 PASS |
| PlayMode | 1/1 PASS |
| QATestRunner | 80/80 PASS |
| Console Error | 0 |

## 구조적 회고

- Room 방향은 연결 메타데이터로만 유지하고 Portal 위치·외형과 분리하는 편이 레벨 디자인 자유도가 높다.
- Portal trigger geometry와 착지 collider를 별도 정량 계약으로 검증해야 지형 매몰을 방지할 수 있다.
- 자산 총개수 고정 Assert보다 역할·직렬화·접근성 기반 Assert가 변화에 강하다.
- Unity Editor 단일 소유권과 구현→자산→QA 순차 공정은 MCP 재연결과 TestRunner 충돌을 줄였다.

## 다음 주 계획

- 실제 입력 기반 Portal 착지·왕복 PlayMode 하네스
- Stage 1 Room별 전투·탐험 밀도 조정
- Far/Near Background와 Minimap 최종 아트
- Hub Inventory/SkillTree 데이터 모델 확정 후 Production UI 확장

