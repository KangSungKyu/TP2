# 2026-08-07 일일 작업 보고서

## 주요 성과

- Stage 1 Portal 생명주기를 `OwnerSlotIdx`와 `RoomGeneration`으로 격리해 stale target 7 전환 경고를 제거했다.
- 11개 Room의 44개 Portal을 메트로배니아형 동선으로 재배치하고 고지대에 solid landing과 접근 발판을 구성했다.
- Portal trigger 매몰을 제거하고, Portal 중심 `surface + 1.0m`, EntryMarker `surface + 0.51m` 계약을 44/44 적용했다.
- Main HUD에 HP/MP/Posture 배경과 current/max 수치 텍스트를 추가하고 Stage Progress UI를 비활성화했다.
- M 키 Production Minimap, 12개 Room View, unknown/visited/cleared/current/boss 상태 표시를 구성했다.
- Unit, Effect, Tilemap, WorldUI SortingLayer와 SortingGroup 계약을 정규화했다.
- SuperArmor 활성 시 피해·자세는 유지하면서 knockback만 차단하도록 공통 경로를 수선했다.

## 세부 작업

### 전투·생명주기

- 처형 HP 0 사망, Groggy 행동 중단, 사망 fade 중 공격 취소 계약을 보강했다.
- Knockback을 0.15초 override로 제한하고 SuperArmor on/off Monster·Boss 4조합을 검증했다.
- `SkillExecutor`의 Particle Addressables 비동기 완료 전 null 검사 경합을 제거했다.

### Stage·Room·Portal

- 방향별 Portal 종류를 제거하고 공용 `Portal_Gate`와 명시적 `TargetSlotIdx`만 사용했다.
- `byte.MaxValue` 자동 목적지 선택을 제거하고 상호 ConnectionMask가 유효한 연결만 허용했다.
- 고정 Portal을 비활성화하고 런타임 생성 Portal만 사용했다.
- 고지대 착지면을 최소 3×2 solid Ground로 구성하고 끊긴 1~2셀 one-way 구간을 제거했다.
- 최종 Spawn–Portal 최소 거리는 7.8103m, 접근 가능한 socket은 44/44다.

### HUD·Minimap·Rendering

- Player/Boss/Monster fill Image 15개를 공용 2×2 SolidFill sprite로 통일했다.
- Player HUD 배경 3개와 HP/MP/Posture current/max 텍스트를 바인딩했다.
- Minimap은 단일 Canvas와 공용 sprite/font/material을 사용해 drawcall 증가를 억제했다.
- SortingLayer를 FarBackground, NearBackground, Tilemap, Unit, Effect, WorldUI 순으로 등록했다.

## QA 결과

| 구분 | 결과 |
|---|---:|
| 전용 계약 | 4/4 PASS |
| EditMode 제품 게이트 | 112/112 PASS |
| PlayMode | 1/1 PASS |
| QATestRunner | 80/80 PASS |
| Console 제품 Error | 0 |
| target 7 전환 경고 | 0 |

환경상 Unity Editor 포커스를 요구하는 테스트 2건은 `editor_unfocused` timeout으로 제품 게이트에서 분리했다.

## 후속 작업

- 실제 Player 입력 기반 저·중·고 Portal 착지 및 왕복 PlayMode 하네스 추가
- Far/Near Background 실제 아트 자산 제작과 배치
- Minimap 아이콘의 최종 프로덕션 아트 교체
- Unity 포커스에 의존하지 않는 테스트 입력 경로 정리

