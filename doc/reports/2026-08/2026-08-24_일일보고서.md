# 📅 2026-08-24 (월) 일일 작업 보고서

## 💡 오늘의 주요 성과 (Executive Summary)

- Player `3001`과 Boss `3201`에 `ReverseVerticalUpswing` 표현을 적용하고 기존 피해·hit timing·AttackCollider 계약은 유지했다.
- 외부 AI Dummy Attack Effect 최초 15종, 방향성 rework 9종, rework V2 5종을 단계적으로 감사·완료했다.
- Effect `8014~8030`, Resource `1081~1097`을 texture·animation·controller·prefab·Addressables로 실제 통합했다.
- 시간은 SkillData hit window, 공간은 EffectData 직렬화 Active bounds가 권위인 Effect–Hitbox 파이프라인을 구현했다.
- `SkillExecutor` uint 매핑, 다단 hit 분리, dedupe, 비동기 spawn과 window 종료 pool cleanup을 완료했다.
- 최종 focused EditMode `2/2`, 대표 PlayMode `1/1`, 제품 Console Error `0`을 확인했다.
- CI commit `a9235c5`를 생성했으나 현재 branch는 upstream보다 4커밋 앞서 있어 push 확인이 남아 있다.

## ⚔️ 1. 공격 모션·판정 구조

| 대상 | 변경 | 유지 계약 |
| :--- | :--- | :--- |
| Player `3001` | 후방 저점 → 머리 → 전방 상단 `ReverseVerticalUpswing` | 기존 피해·hit timing·State 7 |
| Boss `3201` ComboSlash | 수평 중심 표현을 Down→Upswing 조합으로 교체 | 기존 다단 순서·State 11 |

- 주요 반영 파일은 `Assets/Anims/Player/Player_Attack_Hit1.anim`, `Assets/Anims/Monster/Garon_Pattern_ComboSlash.anim`이다.
- CSV 피해와 공격 window를 이펙트 프레임에 맞춘다는 이유로 임의 변경하지 않았다.
- `AttackSubject`의 Weapon/BodyPart/Projectile 책임을 분리하고 Unit `3103` Torso Ram 경로를 통합했다.
- Body/Defense collider는 공격 판정에 재사용하지 않고 전용 공격 collider와 sweep을 사용한다.

## 🗡️ 2. WeaponVisual 제거 요청 공수

| 항목 | 결과 |
| :--- | :--- |
| 제거 영향 감사 | 완료 |
| 예상 공수 | `2.75~4.75h` |
| 실제 제거 | 미수행 |
| 현재 상태 | 7개 Unit Prefab에 `WeaponVisual` 존재 |

- `WeaponVisual`은 표시 전용이며 판정 권위는 AttackCollider와 EffectData bounds다.
- 7개 Prefab child·serialized reference를 제거하는 최소 작업안만 산정했다.
- commit `a9235c5`에는 Unit `3001`, `3101~3105`, `3201` Prefab 변경과 WeaponVisual 직렬화가 포함되므로 제거 완료로 기록하지 않는다.

## 🎨 3. 외부 AI Effect 발주·감사·재작업

| 단계 | Commit | 결과 |
| :--- | :--- | :--- |
| 최초 15종 | `2ba8a66` | Line/Arc/DirectedBox/Ring 시안 완료 |
| 최초 감사 | — | PASS 6 / REWORK 5 / REJECT 4; manifest SHA-256 `0/15` 불일치 |
| 방향성 rework 9종 | `7219bea` | Draw 1–4 / Erase 5–8, frame 8 투명, 안전 여백 반영 |
| Rework V2 5종 | `db5d3f0` | type·파일명 3종과 독립 2-hit 2종 최종 보정 |

V2 대상은 `3105/P6006 Line`, `3201/P6100 Arc`, `3201/P6102 DirectedBox`, `3001/S7003` 2-hit, `3201/P6101` 2-hit다. 시각 trail은 Collider·Damage를 갖지 않으며 PNG alpha scan, `Renderer.bounds`, 문자열 lookup을 런타임 판정에 사용하지 않는다.

## 🗃️ 4. Effect·Resource 실제 통합

| 데이터/자산 | 최종 범위 | 계약 |
| :--- | :--- | :--- |
| EffectData | `8014~8030` | hit tick별 승인 Active `centerX/Y`, `sizeX/Y` |
| ResourceData | `1081~1097` | `Effect_8014~Effect_8030` FK |
| Texture | `AttackEffect_8014~8030.png/.meta` | PPU100, Point, slicing·alpha |
| Animation/Controller | `AttackEffect_8014~8030` | pool 재생 시 normalized time 0 |
| Prefab/Addressables | `Effect_8014~8030` | ResourceManager·EffectPoolManager 경로 |
| 다단 Effect | `7003: 8027→8028`, `6101: 8029→8030` | hit별 독립 Effect와 bounds |

- 시간 권위는 SkillData `HitTimings/HitWindowPre/HitWindowPost`, 공간 권위는 해당 tick의 EffectData Active bounds다.
- FaceLeft는 승인 center X만 반전하고 size는 유지한다.
- bounds 또는 Resource FK가 invalid이면 body fallback 없이 해당 tick을 취소한다.
- 시각 Effect 생성 실패는 유효한 판정 데이터의 피해 자체를 차단하지 않는다.

## 🧩 5. 코드·테스트 변경 범위

| 영역 | 주요 파일·책임 |
| :--- | :--- |
| 데이터 | `Commons.cs`, `EffectDataTable.cs`, `SkillDataTable.cs` — bounds·유효성·다단 데이터 |
| 실행 | `SkillExecutor.cs` — uint 매핑, hit bounds, spawn/dedupe/cleanup |
| Hitbox | `UnitAttackHitbox2D.cs`, `UnitBase.cs` — subject별 collider·sweep 수명주기 |
| Pool | `EffectPoolManager.cs`, `SkillEffect.cs` — 멱등 반환·활성 목록 정리 |
| Unit | `Monster.cs`, `Player.cs`, `KinematicMotor2D.cs` — pattern·motion·cancel 연동 |
| EditMode | `GameplayArchitectureTests.cs`, `CSVDataPipelineTests.cs` |
| PlayMode | `Pass10AttackEffectPlayModeTests.cs` 신규 |

`SkillExecutor`는 유닛/Pattern/Skill/hit tick의 uint 조합으로 Effect를 선택한다. 정상 종료, cancel, stale generation, late completion은 window가 소유하는 공통 반환 경로를 사용하고 `activeEffects.Remove`로 중복 enqueue를 차단한다.

## 🔬 6. QA 최종 결과와 중간 실패 분류

| 게이트 | 최종 결과 | 판정 |
| :--- | :---: | :--- |
| Focused EditMode | `2/2` | PASS |
| 대표 PlayMode | `1/1` | PASS |
| 제품 Console Error | `0` | PASS |

최종 PlayMode Assert:

- Effect `8014` 실제 Addressables/pool spawn
- FaceLeft Active bounds mirror
- renderer 비활성 상태 실제 피해
- window 종료 후 `activeSelf=false`와 pool 반환
- 두 번째 spawn 동일 identity 재사용과 normalized time 0
- cancel 이후 활성 공격 Effect 0

중간 실패 분류:

1. target HP `100→100`: 무지면 fixture의 KinematicMotor 낙하로 교차가 사라진 좌표 결함이며 제품 damage 경로 결함은 아니었다.
2. window 종료 후 Effect 활성 잔존: 제품 수명주기 결함으로, spawn/cleanup UniTask를 window가 소유하고 반환 완료까지 await하도록 수선했다.
3. 재사용 검사 오판: Response `8013`을 Attack `8014`로 선택한 fixture identity 결함으로, uint Effect→Resource pool key 선택으로 해결했다.
4. 최종 재게이트에서 대표 계약 `1/1` PASS를 확인했다.

## 📦 7. Git 마감 결과

| 항목 | 결과 |
| :--- | :--- |
| Branch | `feature/monster-pattern-lifecycle-reservation` |
| 최종 commit | `a9235c5bb8360cab630477412ebb2e5c4e727489` |
| Commit 시각 | `2026-08-24 19:08:33 KST` |
| 제목 | `feat: integrate attack effect hitbox pipeline` |
| 변경 규모 | 188 files, `+15,604/-445` |
| Upstream | `origin/feature/monster-pattern-lifecycle-reservation` |
| 현재 상태 | upstream보다 4커밋 ahead — push 확인 필요 |

2026-08-24 Git 이력:

1. `2ba8a66` — 최초 Dummy Attack Effect 15종
2. `7219bea` — 방향성 rework 9종
3. `db5d3f0` — rework V2 5종
4. `a9235c5` — Effect–Hitbox pipeline 및 실제 Unity 자산 통합

## 🔮 8. 후속 위험 및 다음 작업

1. CI가 push 성공과 원격 commit `a9235c5` 도달 여부를 확인해야 한다.
2. working tree에는 `a9235c5` 이후 기존 dirty와 미추적 파일이 남아 있다. broad stage·reset·clean 없이 범위를 재감사해야 한다.
3. 최종 QA는 focused EditMode `2/2`와 대표 Effect `8014` PlayMode `1/1` 범위다. `8015~8030` 개별 PlayMode 전수 증거는 별도 게이트가 필요하다.
4. 시각 자산 교체 시 프레임 외형이 아니라 승인 EffectData bounds를 재계측·승인해야 한다.
5. WeaponVisual 제거는 공수 산정만 완료됐다. 재개 시 7개 Prefab과 nullable 참조를 별도 처리해야 한다.

### 🔄 [PM/CI 동기화] 변경 이력 테이블

| 최근 수정 일시 | 수정자 (역할) | 수정 및 추가된 파일/메서드 명세 | QA 검증 통과 기준 (Assert) |
| :--- | :--- | :--- | :--- |
| 2026-08-25 | 문서작업자 | `2026-08-24_일일보고서.md`를 commit `a9235c5`, 실제 188파일 범위, effect rework/integration 및 최종 QA 근거로 재작성 | Git 이력·경로 확인, EditMode 2/2, PlayMode 1/1, 제품 Error 0, diff-check PASS |
